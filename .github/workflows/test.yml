name: Test

on:
  push:
    branches: ['master']
  pull_request:
    branches: [master]
jobs:
  Test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        directory: ['packages/a', 'packages/b']

    steps:
      - uses: actions/checkout@v2

      - run: npm run test
